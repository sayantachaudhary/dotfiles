#!/bin/sh

emojis="$HOME/.local/share/emoji.txt"

moji=$(rofi -dmenu -i -p "Emojis:" <"$emojis")
[ -z "$moji" ] && exit 0

selected=$(printf '%s\n' "$moji" | awk '{print $1}')

if [ -n "$WAYLAND_DISPLAY" ]; then
	printf "%s" "$selected" | wl-copy
else
	printf "%s" "$selected" | xclip -sel clip
fi
